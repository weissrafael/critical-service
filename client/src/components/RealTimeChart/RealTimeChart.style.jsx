import styled from 'styled-components'
import {spacing, colors, text, fontWeights, layoutMaxWidth} from "../../styles/styleGuide";

export const ChartContainer = styled.div`
  height: ${layoutMaxWidth};
  width: ${layoutMaxWidth};
`
